# Points at the SAME Supabase project as the main ProductHub app — this
# console reads/writes cross-tenant data via the platform_admins RLS
# policies added in ProductHub's supabase/migrations/0025_platform_admin.sql.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
